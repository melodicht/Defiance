import { DragEventDirective } from './drag-event.directive';

describe('DragEventDirective', () => {
  it('should create an instance', () => {
    const directive = new DragEventDirective();
    expect(directive).toBeTruthy();
  });
});
